#
# Optional integrations (the app should run even if these are unset)
#

# --- Supabase (optional) ---
# If set, waitlist submissions can be stored in Supabase.
# Prefer SERVER-SIDE keys only (never expose service role key to the client).
# SUPABASE_URL="https://xxxx.supabase.co"
# SUPABASE_SERVICE_ROLE_KEY="..."

# --- Discord webhook (optional) ---
# If set, a Discord message will be posted on new waitlist submissions.
# DISCORD_WEBHOOK_URL="https://discord.com/api/webhooks/..."

# --- PostHog (optional, client) ---
# If set, client-side analytics events are captured.
# NEXT_PUBLIC_POSTHOG_KEY="phc_..."
# NEXT_PUBLIC_POSTHOG_HOST="https://app.posthog.com"

# --- Sanity (optional) ---
# If set, landing page content can be fetched from Sanity.
# SANITY_PROJECT_ID="..."
# SANITY_DATASET="production"
# SANITY_API_VERSION="2024-01-01"

# --- Admin access (optional) ---
# If set, /admin requires this token (query param ?token=...).
# ADMIN_TOKEN="change-me"
